Modify ONLY the "My Activity Tracker" page and remove "Activity Log Portal" from the sidebar in both Coordinator and Lecturer views. Touch nothing else.
EXACT DESIGN SPECIFICATIONS to follow — do not deviate:
Typography & Colours (match exactly from existing pages):

Page title font: large bold dark/black text (same as "My Activity Tracker", "Staff Activity Tracker", "Export Reports" titles)
Page subtitle: small grey regular text below the title
Background: light grey (#F5F5F5 or matching existing page background)
Active tab underline + active tab text: dark maroon/crimson (matching the existing sidebar active item colour, the "View Pending Queue" button, and the "Generate Full Report" button — the same deep red used throughout the prototype)
Inactive tab text: medium grey, no underline
Tab bar bottom border: light grey divider line separating tabs from content below

Change 1 — My Activity Tracker page header (BOTH views):

Keep the page title "My Activity Tracker" in the same position and style as it currently is
Keep the subtitle "Log your research, grants, and service activities (visible to coordinators and staff)" exactly as is
REMOVE the dark blue "+ Add New Activity" button from the top right — delete it entirely
Directly below the subtitle, add a tab row with exactly two tabs:

Tab 1 label: "My Activities" — this is the DEFAULT active tab
Tab 2 label: "Add Activity"
Tab styling: plain text tabs, no background fill on tabs, just an underline indicator on the active tab in dark maroon, same font size as filter section labels
Add a thin light grey horizontal divider line below the tab row



Tab 1 content — "My Activities" (default view):

Show exactly what is currently on the My Activity Tracker page unchanged:

The white "Filter Activities" card with Type dropdown, Mode dropdown, From Date field, To Date field — all exactly as they currently appear
The 4 summary stat cards in a row: "Total Activities / 3", "Research Projects / 1" (purple number), "Active Grants / 1" (orange/gold number), "Total Hours / 215" (black number) — same card style as existing white cards with light border
The activity list table with columns: TITLE, TYPE, HOURS SPENT, MODE, DATE SUBMITTED, ACTIONS — same styling as existing tables in the prototype (light grey column headers in small caps, white rows, thin dividers between rows)
Activity rows with coloured type badges: "Research" in purple/lavender, "Grant" in yellow/gold, "Service" in light blue — same pill badge style as currently shown
Edit (pencil) and Delete (trash) icons in the ACTIONS column in the same muted red/pink colour as currently shown



Tab 2 content — "Add Activity":

This tab shows the full content from the existing "Activity Log Portal" page, laid out exactly as it currently appears on that page:

Left card: "Automated ORCID Fetch" — white card, bold section title, dark maroon "Fetch from ORCID" button with upload icon on the left, same button style as "Generate Full Report" and "View Pending Queue" buttons, grey description text below
Right card: "Manual Entry" — white card, bold section title, "Activity Title" text input field with placeholder "Enter activity title", "Date" field with mm/dd/yyyy placeholder, "Activity Category" dropdown with "Select category" placeholder, green "Save Activity" button (same green as the Workload Compliance bar optimal colour)
Below both cards, a full-width white card: "Manual Research Paper Upload" with red document icon and bold title, grey subtitle text, "Paper Title" required field (red asterisk), "Authors" field, "Publication Date" required field, "Journal / Conference" field, PDF drag-and-drop upload zone with upload icon and "Click to upload or drag and drop" text and "PDF only · max 20 MB" subtext, dark maroon "Submit Research Paper" button with upload icon
All spacing, card padding, and layout must match exactly how Activity Log Portal currently looks



Change 2 — Sidebar navigation (BOTH Coordinator and Lecturer views):

Remove "Activity Log Portal" list item from the left sidebar in both views
Do not reorder, restyle, or touch any other sidebar items
Lecturer sidebar after change: Dashboard, My Queue, My Activity Tracker (and any items below it that already exist)
Coordinator sidebar after change: all existing coordinator sidebar items remain, minus "Activity Log Portal" only

Change 3 — Activity Log Portal page:

Do not delete the frame, just leave it unlinked. Remove it from sidebar navigation only.

DO NOT change anything else:

Do not touch the Dashboard, My Queue, Course & Moderator Manager, Student Segmentation, Lab Resource Planner, Assignment Tool, Assignment Status Board, Staff Activity Tracker, Export Reports, Profile & ORCID Setup, or Login pages
Do not change the top navigation bar, UTM logo, search bar, notification bell, or user profile area
Do not change any fonts, colours, spacing, or component styles globally
Do not change the Coordinator view's My Activity Tracker stats (Total Activities: 9, Research Projects: 3, Active Grants: 2, Total Hours: 685) or table content